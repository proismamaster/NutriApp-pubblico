// Rimosso dal tracking git in questo commit — stub di login mai collegato
// (login reale in lib/domain/user_provider.dart). Vedi PROBLEMS.md.
//
// Il file fisico non è stato eliminabile dalla sandbox che ha fatto la
// pulizia (stesso blocco di permessi già visto sui lock .git). È sicuro
// cancellarlo a mano da Windows: dopo questo commit non è più tracciato.
